import SwiftUI

struct CalendarView: View {
    var body: some View {
        VStack {
            Text("캘린더 뷰 (추후 디자인 적용 예정)")
                .font(.title)
                .padding()

            Spacer()
        }
    }
}

#Preview {
    CalendarView()
}
